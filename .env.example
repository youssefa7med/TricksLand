# ============================================================
# Treeks Land — Environment Variables
# Copy this file to .env.local and fill in your values.
# ============================================================

# ---- Supabase (Frontend) ------------------------------------
# Found in: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here

# ---- App URL ------------------------------------------------
# Your deployment URL (used for auth redirects)
# Local dev: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================
# The following variables are used ONLY in Supabase Edge
# Functions (server-side). Never expose them in the frontend.
# Set them in: Supabase Dashboard → Edge Functions → Secrets
# ============================================================

# Supabase service role key — full DB access, keep secret
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Resend API key — for sending invoice emails
# Get one at https://resend.com
RESEND_API_KEY=re_your_resend_api_key_here

# Admin email — receives the monthly summary email
ADMIN_EMAIL=admin@youracademy.com

# App URL for Edge Functions (same value as NEXT_PUBLIC_APP_URL)
APP_URL=http://localhost:3000
